version: "3.9"

services:
  ai_service:
    build: .
    container_name: ai_service
    ports:
      - "5001:5001"
    volumes:
      - ./uploads:/app/uploads
      - ./static:/app/static
    restart: unless-stopped
